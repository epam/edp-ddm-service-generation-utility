apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: {{ .Values.datafactoryResponseCeph.bucketName }}
spec:
  additionalConfig:
    bucketclass: registry-bucket-class
  generateBucketName: {{ .Values.datafactoryResponseCeph.bucketName }}
  storageClassName: registry-bucket
