apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: {{ .Values.datafactoryFileCeph.bucketName }}
spec:
  additionalConfig:
    bucketclass: registry-bucket-class
  generateBucketName: {{ .Values.datafactoryFileCeph.bucketName }}
  storageClassName: registry-bucket
  ssl: false